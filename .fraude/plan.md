# Implementation Plan

*No plan yet.*



## Plan:

1. **Create a new helper method `quadratic_formula`** in `combined.py` to calculate the roots of a quadratic equation (ax^2 + bx + c = 0).
2. **Modify the `run` function** in `combined.py` to include an example usage of the `quadratic_formula` method.
3. **Add necessary imports** (e.g., `math` for `sqrt`) if not already present.

